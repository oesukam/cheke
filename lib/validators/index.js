const fs=require("fs"),path=require("path"),basename=path.basename(__filename),validators={};fs.readdirSync(__dirname).filter(a=>0!==a.indexOf(".")&&a!==basename&&".js"===a.slice(-3)).forEach(a=>{validators[a.slice(0,-3)]=require(path.join(__dirname,a))}),module.exports=validators;